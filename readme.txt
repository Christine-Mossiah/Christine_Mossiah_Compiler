ReadMe
Christine Mossiah (Almodovar) CSc 420  Spring '16
In order to test each function, you have to copy and paste each example to the input.txt file then run the compiler.
This will show a token list, symbol table, and instruction list.  Then you will see the proper output of the program.
There is then the symbol table again after the program has executed.

There are screen shots of the if-then-else statement, for loop, while loop, and a repeat function all in a screen shots folder.  